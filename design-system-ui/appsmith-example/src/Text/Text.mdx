import { Canvas, Meta } from "@storybook/blocks";

import * as TextStories from "./Text.stories";

<Meta of={TextStories} />

# Text

{/* Add a short sentence here describing the general use of this component. */}

<Canvas of={TextStories.TextStory} />

Use the text component for displaying textual information to the user (as headings or body)

- Only use text tokens (text styles in figma) when the Text component doesn’t meet your need.
- Use sentence case. No capital letters.

To know more about how to use typography, ref [Typography](?path=/story/design-system-text--text-story)
